<% provide(:title, "News")%>
<h1>News</h1>
<div class="row">
  <%= render 'form' %>
  <ul id="newsfeeds" class="newsfeeds">
    <%= render partial: 'newsfeed', collection: @newsfeeds %>
  </ul>
</div>
